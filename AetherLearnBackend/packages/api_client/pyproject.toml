[project]
name = "api-client"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27.0", "shared-schemas"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
