[project]
name = "shared-events"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["nats-py>=2.7.0", "orjson>=3.10.0", "pydantic>=2.8.0", "shared-schemas"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
