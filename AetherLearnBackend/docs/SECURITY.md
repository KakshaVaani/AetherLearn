# Security

- Argon2id password hashing
- JWT access tokens and rotating refresh tokens
- HMAC service signatures on internal requests
- Signed user context headers
- Role checks in gateway and services
- No password hashes or refresh tokens in API responses
- No raw source image logs
- Source images are not stored by default
- Commons publishing requires review
