[project]
name = "aetherlearn-ai-service"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["shared-schemas", "shared-utils", "shared-events", "service-auth", "fastapi", "httpx", "pillow", "pymongo"]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
