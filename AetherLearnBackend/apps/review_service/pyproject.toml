[project]
name = "aetherlearn-review-service"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["shared-schemas", "shared-utils", "shared-events", "service-auth", "fastapi", "pymongo"]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
