[project]
name = "aetherlearn-export-service"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["shared-schemas", "shared-utils", "shared-events", "service-auth", "fastapi", "reportlab", "pymongo"]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
